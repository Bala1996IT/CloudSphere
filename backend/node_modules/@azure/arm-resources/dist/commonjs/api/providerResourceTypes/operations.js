var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  _listDeserialize: () => _listDeserialize,
  _listSend: () => _listSend,
  list: () => list
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../models/models.js");
var import_urlTemplate = require("../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
function _listSend(context, resourceProviderNamespace, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/subscriptions/{subscriptionId}/providers/{resourceProviderNamespace}/resourceTypes{?api%2Dversion,%24expand}",
    {
      subscriptionId: context.subscriptionId,
      resourceProviderNamespace,
      "api%2Dversion": context.apiVersion ?? "2025-04-01",
      "%24expand": options?.expand
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: { accept: "application/json", ...options.requestOptions?.headers }
  });
}
async function _listDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    if (result.body) {
      error.details = (0, import_models.cloudErrorDeserializer)(result.body);
    }
    throw error;
  }
  return (0, import_models.providerResourceTypeListResultDeserializer)(result.body);
}
async function list(context, resourceProviderNamespace, options = { requestOptions: {} }) {
  const result = await _listSend(context, resourceProviderNamespace, options);
  return _listDeserialize(result);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  _listDeserialize,
  _listSend,
  list
});
//# sourceMappingURL=operations.js.map
