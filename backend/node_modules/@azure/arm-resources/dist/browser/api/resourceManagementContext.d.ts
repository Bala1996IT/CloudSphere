import { AzureSupportedClouds } from "../static-helpers/cloudSettingHelpers.js";
import { Client, ClientOptions } from "@azure-rest/core-client";
import { TokenCredential } from "@azure/core-auth";
/** Provides operations for working with resources and resource groups. */
export interface ResourceManagementContext extends Client {
    /** The ID of the target subscription. The value must be an UUID. */
    subscriptionId: string;
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion?: string;
}
/** Optional parameters for the client. */
export interface ResourceManagementClientOptionalParams extends ClientOptions {
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion?: string;
    /** Specifies the Azure cloud environment for the client. */
    cloudSetting?: AzureSupportedClouds;
}
/** Provides operations for working with resources and resource groups. */
export declare function createResourceManagement(credential: TokenCredential, subscriptionId: string, options?: ResourceManagementClientOptionalParams): ResourceManagementContext;
//# sourceMappingURL=resourceManagementContext.d.ts.map